import * as DNDCore from '@dnd-kit/core';
import * as DNDSortable from '@dnd-kit/sortable';
import * as DNDUtilities from '@dnd-kit/utilities';

const DragAndDrop = { DNDCore, DNDSortable, DNDUtilities };

export default DragAndDrop;
